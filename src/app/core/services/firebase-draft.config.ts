import { environment } from '../../../environments/environment';

export const firebaseDraftConfig = {
  ...environment.firebase,
  notes: 'Replace placeholders with real Firebase project info when available.'
};
